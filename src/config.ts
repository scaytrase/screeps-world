export const HARVESTERS_COUNT = 14;
export const HARVESTER_BODY = [WORK, CARRY, CARRY, MOVE, MOVE];

export const BUILDERS_COUNT = 3;
export const BUILDERS_ENERGY_LIMIT = 100;
export const BUILDER_BODY = [WORK, WORK, CARRY, MOVE, MOVE];

export const UPGRADERS_COUNT = 1;
export const UPGRADERS_ENERGY_LIMIT = 200;
export const UPGRADER_BODY = [WORK, CARRY, MOVE];

export const REPAIRERS_COUNT = 1;
export const REPAIRERS_ENERGY_LIMIT = 50;
export const REPAIRER_BODY = [WORK, CARRY, MOVE];

export const TTL_UNTIL_RENEW = 400;
