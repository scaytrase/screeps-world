export const HARVESTERS_COUNT = 12;
export const HARVESTER_BODY = [WORK, WORK, CARRY, CARRY, MOVE, MOVE];

export const BUILDERS_COUNT = 4;
export const BUILDERS_ENERGY_LIMIT = 150;
export const BUILDER_BODY = [WORK, WORK, CARRY, CARRY, MOVE, MOVE];

export const UPGRADERS_COUNT = 2;
export const UPGRADERS_ENERGY_LIMIT = 150;
export const UPGRADER_BODY = [WORK, WORK, CARRY, CARRY, MOVE, MOVE];

export const REPAIRERS_COUNT = 3;
export const REPAIRERS_ENERGY_LIMIT = 150;
export const REPAIRER_BODY = [WORK, WORK, CARRY, MOVE, MOVE];

export const SPAWN_KEEPERS_COUNT = 1;
export const SPAWN_KEEPER_BODY = [CARRY, CARRY, MOVE, MOVE];

export const TTL_UNTIL_RENEW = 100;
export const TTL_RENEW_LIMIT = 600;
export const USE_PERMANENT_SAFE_MODE = true;
export const RENEW_CREEPS = false;
